RCN MARKETPLACE - Deploy-ready (placeholder for OPENAI key)


This package includes a React + Vite frontend scaffold connected to Firebase
and a simple serverless API scaffold (api/support.js) that expects an
OPENAI_API_KEY and SUPPORT_EMAIL to be set in the platform environment.

Files included:
- package.json, vite.config.js
- src/ (App, pages, components)
- src/firebase.js (configured with the Firebase project you provided)
- api/support.js (serverless scaffold)
- README with deploy steps in /DEPLOY_STEPS.txt

Important: Do NOT commit your real secret keys to the repo. Set them in Vercel Environment Variables.
